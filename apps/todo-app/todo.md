-e[x] Make it so that when i check a todo, it triggers the history query to be updated without me refreshing

- [x] Create an api-hooks directory
- [x] Fix naviagation back to parent app in embedded provider
- [x] Implement a tab bar on mobile instead of a sidebar
- [x] Add a confirmation before deleting todos forever on the home and history pages.

- [ ] Why doesn't client side nav seem to work the first time we navigate to tab.
- [ ] Implement way to assign dates to todos in the future
  - Start with just a date picker that you can add a date via clicking a small Calendar icon next to add
  - Use todoist way to type in `in two day` or `mon`
- [ ] Fix mobile ux issues
